import React from 'react';
import ToDo from './ToDo/ToDo';

const ToDos = () => {
  const toDoList = ['foo'];
  return (
    <div>bla</div>
  )
};

export default ToDos;





















    // <ul>
    //   {toDoList.map( (item, i) => (
    //     <ToDo key={i} item={item} />
    //   ))}
    // </ul>